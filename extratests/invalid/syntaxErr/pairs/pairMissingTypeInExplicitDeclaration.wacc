# Missing second type in explicit pair type declaration

# Output:
# #syntax_error#

# Exit:
# 100

# Program:

begin
  pair(int, int) p = newpair(1, 2) ;
  pair(pair(int, ), int) q = newpair(p, 3)
end
